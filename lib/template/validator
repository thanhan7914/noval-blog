const Validator = require('${data.path}');

class ${data.name} extends Validator {
    validate(value) {
        return {
            passed: true,
            message: 'successful'
        };
    }
}

module.exports = ${data.name};
